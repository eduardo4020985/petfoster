/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AnimalOngComponent } from './animal-ong.component';

describe('AnimalOngComponent', () => {
  let component: AnimalOngComponent;
  let fixture: ComponentFixture<AnimalOngComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [AnimalOngComponent]
    });
    fixture = TestBed.createComponent(AnimalOngComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */